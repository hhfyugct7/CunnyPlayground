<?xml version="1.0" encoding="utf-8"?>
<!-- Compact OriginIsland media player (custom SuperX template). Background is transparent — the
     OriginOS card provides the surface (no glow). Icons are custom vectors. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/media_player_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="#00000000"
    android:paddingStart="20dp"
    android:paddingEnd="20dp"
    android:paddingTop="18dp"
    android:paddingBottom="14dp">

    <!-- Header: album art + title/artist -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageView
            android:id="@+id/media_album_art"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_media_placeholder"
            android:background="@drawable/bg_origin_chip"
            android:clipToOutline="true" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="14dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/media_track_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#FFFFFF"
                android:textSize="17sp"
                android:textStyle="bold"
                android:singleLine="true"
                android:ellipsize="marquee"
                android:marqueeRepeatLimit="marquee_forever" />

            <TextView
                android:id="@+id/media_artist_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#A6FFFFFF"
                android:textSize="14sp"
                android:layout_marginTop="2dp"
                android:singleLine="true"
                android:ellipsize="end" />
        </LinearLayout>
    </LinearLayout>

    <!-- Progress: current · bar · total -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/media_time_current"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#A6FFFFFF"
            android:textSize="11sp"
            android:minWidth="34dp"
            android:text="00:00" />

        <ProgressBar
            android:id="@+id/media_progress_bar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="6dp"
            android:layout_marginStart="10dp"
            android:layout_marginEnd="10dp"
            android:progressDrawable="@drawable/media_progress_track"
            android:progressTint="#FFFFFF" />

        <TextView
            android:id="@+id/media_time_total"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#A6FFFFFF"
            android:textSize="11sp"
            android:minWidth="34dp"
            android:gravity="end"
            android:text="00:00" />
    </LinearLayout>

    <!-- Controls: prev · play/pause · next -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:orientation="horizontal"
        android:gravity="center">

        <ImageButton
            android:id="@+id/media_btn_prev"
            android:layout_width="46dp"
            android:layout_height="46dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_media_prev"
            android:scaleType="fitCenter"
            android:padding="9dp"
            android:layout_marginEnd="28dp" />

        <ImageButton
            android:id="@+id/media_btn_play_pause"
            android:layout_width="54dp"
            android:layout_height="54dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_media_play"
            android:scaleType="fitCenter"
            android:padding="6dp" />

        <ImageButton
            android:id="@+id/media_btn_next"
            android:layout_width="46dp"
            android:layout_height="46dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_media_next"
            android:scaleType="fitCenter"
            android:padding="9dp"
            android:layout_marginStart="28dp" />
    </LinearLayout>

</LinearLayout>
